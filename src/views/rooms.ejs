<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room Dashboard - Message API</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/room.css">
</head>
<body class="dashboard-layout">
    <div class="container">
        <div class="header">
            <h1>Welcome, <%= username %>!</h1>
            <a href="/logout" class="logout-btn">Logout</a>
        </div>

        <div class="grid">
            <div class="room-list">
                <h2>Your Rooms</h2>
                <% if (locals.userRooms && userRooms.length > 0) { %>
                    <% userRooms.forEach(function(room) { %>
                        <div class="room-card">
                            <a href="/message?room=<%= room.name %>" class="room-link">
                                <span class="room-name"><%= room.name %></span>
                            </a>
                            <% if (parseInt(room.ownerId) === parseInt(userId)) { %>
                                <div class="owner-actions">
                                    <button class="btn-icon btn-edit"
                                            data-id="<%= room.id %>"
                                            data-name="<%= room.name %>"
                                            onclick="openEditModal(this)">✏️</button>
                                    <form action="/rooms/<%= room.id %>/delete" method="POST" class="delete-form">
                                        <button type="submit" class="btn-icon btn-delete">🗑️</button>
                                    </form>
                                </div>
                            <% } %>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">You haven't joined any rooms yet.</div>
                <% } %>
            </div>

            <div class="room-list">
                <h2>Available Rooms</h2>
                <% if (locals.availableRooms && availableRooms.length > 0) { %>
                    <% availableRooms.forEach(function(room) { %>
                        <div class="room-card">
                            <form action="/rooms/<%= room.id %>/join" method="POST" class="join-form">
                                <button type="submit" class="join-btn">
                                    <span class="room-name"><%= room.name %></span>
                                </button>
                            </form>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">No other rooms available.</div>
                <% } %>
            </div>
        </div>

        <div class="create-room-section">
            <h2 class="section-title">Create a New Room</h2>
            <form action="/rooms" method="POST">
                <div class="form-group">
                    <label for="roomName">Room Name</label>
                    <div class="input-group">
                        <input type="text" id="roomName" name="name"
                               placeholder="e.g., project-alpha" required
                               pattern="[a-zA-Z0-9-]+"
                               title="Only letters, numbers, and hyphens">
                        <button type="submit">Create</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="modal-overlay" id="editModal">
        <div class="modal-box">
            <h3>Edit Room Name</h3>
            <form id="editRoomForm" method="POST">
                <div class="form-group">
                    <label for="editRoomName">New Name</label>
                    <input type="text" id="editRoomName" name="name"
                           required pattern="[a-zA-Z0-9-]+"
                           title="Only letters, numbers, and hyphens"
                           maxlength="50">
                </div>
                <div class="modal-actions">
                    <button type="button" class="btn-cancel" onclick="closeEditModal()">Cancel</button>
                    <button type="submit" class="btn-save">Save</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function openEditModal(btn) {
            const roomId = btn.dataset.id;
            const name = btn.dataset.name;
            document.getElementById('editRoomForm').action = `/rooms/${roomId}/rename`;
            document.getElementById('editRoomName').value = name;
            document.getElementById('editModal').classList.add('active');
            document.getElementById('editRoomName').focus();
        }

        function closeEditModal() {
            document.getElementById('editModal').classList.remove('active');
        }

        document.getElementById('editModal').addEventListener('click', function(e) {
            if (e.target === this) closeEditModal();
        });
    </script>
</body>
</html>